# Copy this file to application-local.properties and fill with your local credentials.
# DO NOT commit application-local.properties to Git.

# Spring datasource local overrides
spring.datasource.username=root
spring.datasource.password=your_local_mysql_password

# Example: use a different DB URL for local dev
# spring.datasource.url=jdbc:mysql://localhost:3306/powerfit_local
